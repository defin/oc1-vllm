name: Build and push vLLM images

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:

  build-32b:
    runs-on: ubuntu-latest
    permissions: { contents: read, packages: write }
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with: { registry: ghcr.io, username: "${{ github.actor }}", password: "${{ secrets.GITHUB_TOKEN }}" }
      - uses: docker/setup-buildx-action@v3
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          push: true
          tags: ghcr.io/defin/oc1-vllm:32b
          cache-from: type=gha,scope=32b
          cache-to: type=gha,mode=max,scope=32b

  build-3b:
    runs-on: ubuntu-latest
    permissions: { contents: read, packages: write }
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with: { registry: ghcr.io, username: "${{ github.actor }}", password: "${{ secrets.GITHUB_TOKEN }}" }
      - uses: docker/setup-buildx-action@v3
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.3b
          push: true
          tags: ghcr.io/defin/oc1-vllm:3b
          cache-from: type=gha,scope=3b-v2
          cache-to: type=gha,mode=max,scope=3b-v2

  build-14b:
    runs-on: ubuntu-latest
    permissions: { contents: read, packages: write }
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with: { registry: ghcr.io, username: "${{ github.actor }}", password: "${{ secrets.GITHUB_TOKEN }}" }
      - uses: docker/setup-buildx-action@v3
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.14b
          push: true
          tags: ghcr.io/defin/oc1-vllm:14b
          cache-from: type=gha,scope=14b
          cache-to: type=gha,mode=max,scope=14b

  build-7b:
    runs-on: ubuntu-latest
    permissions: { contents: read, packages: write }
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with: { registry: ghcr.io, username: "${{ github.actor }}", password: "${{ secrets.GITHUB_TOKEN }}" }
      - uses: docker/setup-buildx-action@v3
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.7b
          push: true
          tags: ghcr.io/defin/oc1-vllm:7b
          cache-from: type=gha,scope=7b
          cache-to: type=gha,mode=max,scope=7b

  build-qwen3-32b:
    runs-on: ubuntu-latest
    permissions: { contents: read, packages: write }
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with: { registry: ghcr.io, username: "${{ github.actor }}", password: "${{ secrets.GITHUB_TOKEN }}" }
      - uses: docker/setup-buildx-action@v3
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.qwen3-32b
          push: true
          tags: ghcr.io/defin/oc1-vllm:qwen3-32b
          cache-from: type=gha,scope=qwen3-32b
          cache-to: type=gha,mode=max,scope=qwen3-32b

  build-codestral-22b:
    runs-on: ubuntu-latest
    permissions: { contents: read, packages: write }
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with: { registry: ghcr.io, username: "${{ github.actor }}", password: "${{ secrets.GITHUB_TOKEN }}" }
      - uses: docker/setup-buildx-action@v3
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.codestral-22b
          push: true
          tags: ghcr.io/defin/oc1-vllm:codestral-22b
          cache-from: type=gha,scope=codestral-22b
          cache-to: type=gha,mode=max,scope=codestral-22b
